# L4 step 7 — `ecom`

Step 7 of layer **L4** in [`../PLAN.md`](../PLAN.md) §3.5. Status: **TODO**.
Paths are relative to `rewrite/`, the rewrite's tree, as they are in the plan.

---

7. **TODO** — `ecom`: the empirical SRP frame, D/Y/B. Source: Arnold et al. 2015. Gate: the
   published parameterisation reproduced on a GNSS arc.

   *Rule-4 search, 2026-09-24.* Arnold et al. 2015 is obtained as the authors' accepted manuscript
   — green open access, BORIS 69654, fetched through CORE; the publisher, BORIS itself, ResearchGate
   and ADS refused automated access. It prints the D/Y/B frame (Eq. 1), which settles
   `FRAME-Q-002`: `SPEC-frames` §4.7's first-principles e_Y is the same vector. It prints the
   original and reduced ECOM (Eqs. 3–4, argument u) and ECOM2 (Eq. 5, argument Δu = u − u_s, even
   harmonics in D and odd in B), with the nine-parameter D4B1 that CODE adopted on 4 January 2015.
   It prints parameter counts and SLR validation statistics — **no worked acceleration**, and no
   case this layer can reproduce without estimating.

   *Gate replaced 2026-09-24.* "The published parameterisation reproduced on a GNSS arc" means
   fitting ECOM's parameters to observations — L7's estimator on L6's reader — so it is not a gate
   here (§3.5's principle). The gate is what the source's own formulas imply, at parameters stated
   in the test: the frame's properties as Eq. 1 defines them; Δu independent of the coordinate
   system, as the paper states; D periodic over 180° and B − B₀ antiperiodic, which is what even
   and odd harmonics mean; the reduction at n_D = 0, n_B = 1 to Eq. 4 — written in u, not Δu —
   once its two once-per-revolution coefficients are rotated by u_s, which checks Δu's definition
   against a second printed formula; each parameter's sensitivity column against finite
   differences; and the velocity dependence through the orbital plane, declared with its Jacobian
   or bounded by a number (`DYN-Q-002`). Eq. 5 transcribed a second time in a test is a regression
   value, not evidence. What needs estimating — Arnold's Y₀ = 0 at β = ±90°, and the frozen `E-*`
   residuals — is L7's.
