# L4 step 6 — `thrust-yaw`

Step 6 of layer **L4** in [`../PLAN.md`](../PLAN.md) §3.5. Status: **TODO**.
Paths are relative to `rewrite/`, the rewrite's tree, as they are in the plan.

---

6. **TODO** — `thrust-yaw`: antenna thrust and the yaw-attitude laws. Sources: Steigenberger
   2018; Kouba 2009; Montenbruck et al. 2015; the official Galileo, GLONASS and BeiDou
   attitude-law documents. Gate: published yaw angles through noon and midnight turns, per
   constellation.

   *Scope boundary set 2026-09-23.* Step 5 creates the `attitude` module with the **ideal nominal
   yaw-steering law** only — closed-form geometry both force plugins need from the day they exist
   — and that law **refuses** at the degenerate Sun-on-nadir geometry rather than returning a frame
   it cannot define. Everything beyond ideal nominal is this step's: the noon and midnight turns
   the ideal law cannot represent, the constellation-specific laws, and antenna thrust. Step 6
   adds providers to the module step 5 creates; it does not rebuild the nominal law.

   *Scope and order set 2026-09-24, from the rule-4 search and from what the plan's own campaigns
   need.* The frozen GNSS baselines this tree must reproduce are **GPS only** — `G-01`…`G-03` are
   PRNs G01 and G05 in February 2023, and the `B-*` block statistics are GPS IIF, IIR-A/B/M and
   IIIA — and L5's and L8's gates are GPS too. So this step builds **GPS** and gates it; Galileo,
   GLONASS and BeiDou are **carried** as a named follow-on, not dropped, with the sources the
   search found for each recorded (the Galileo operator's metadata page, Dilssner et al. 2011 for
   GLONASS-M, the CSNO standard with a published observed deviation for BeiDou).

   Within GPS, the load-bearing blocks are the ones the baselines use, and **the search's best
   source does not cover them**: Kouba 2009 gives II/IIA and IIR, but IIF launched in 2010 and IIIA
   in 2018. Their laws need their own rule-4 search before the gate is designed.

   *The gate's form, since the sources print formulas and figures rather than numeric yaw tables.*
   Kouba's published noon-turn limits β₀ follow exactly from his published hardware yaw rates:
   the ideal law's peak rate is μ̇/tan β, so tan β₀ = μ̇/R_max, and with GPS's mean motion that
   gives 3.57° to 4.87° for II/IIA's 0.134 to 0.098 °/s and 2.39° for IIR's 0.20 °/s, against the
   printed 3.6–4.9° and 2.4°. The implementation's turn-onset logic must reproduce the published
   β₀ from the published rates — category 1, and independent of transcribing the turn formula.
   Add continuity with the ideal law at each hand-over and |ψ̇| ≤ R_max throughout the turn; and
   where attitude is openly published, it is the numeric form this gate's wording asks for (what
   such a publication carries: see the amendment below).

   *Antenna thrust* is a force **law** here — the recoil of the radiated power, P/c along the
   boresight — with P stated in the test, as the L4/L5 decomposition requires. Per-satellite
   transmit powers are L5's population, so Steigenberger 2018 being unreachable does not block
   this step.

   *Ruled 2026-09-24, after the narrow search.* The baselines' satellites are G01 = SVN 63, Block
   **IIF**, and G05 = SVN 50, Block **IIR-M**, from the IGS satellite metadata SINEX. IIR-M flies
   IIR's law. IIF's two rates are Dilssner 2010's — 0.11 °/s at noon, 0.06 °/s in the shadow — and
   the `eclips` model's 0.06 °/s is cited to that paper, so it confirms the transcription and is not
   a second source. **The form of IIF's midnight turn is open**, and `TYAW-A-009` decides it.

   > **Correction found at L4 step 6: the IIF midnight turn was ruled on more than its source
   > gave.** As first written, this ruling said IIF was "solved from two independent primary
   > sources", put the onset at 7.95°, and set a paraphrase in quotation marks. The sources are one,
   > as above; tan β₀ = μ̇/R at 0.06 °/s gives **7.93°** (atan(0.00836/0.06)); and the words, as the
   > spec quotes them, are "as long as the elevation β of the Sun … is greater than 8 degrees". The
   > passages quoted so far fit **two forms**: a rate-limited turn that leaves the ideal law where
   > its rate reaches 0.06 °/s — what the implementation builds, and the form the 8° fits — or one
   > constant rate spanning the shadow from entry to exit. Each misses one of the paper's own numbers
   > by about 10%: the first takes 180°/0.06 °/s = 50.0 min at β = 0 against its "about 55
   > minutes", and the second lasts the shadow, 55.4 min at β = 0, but at 0.054 °/s rather than
   > 0.06. At β = 4.42° the two part by a third in rate, 0.060 against 0.045 °/s, and by 18 min in
   > when the turn starts. Both predictions are registered before `TYAW-A-009`'s extraction runs;
   > the verdict is relative — each extracted quantity within a quarter of the two forms'
   > separation, set by the manager before the measurement (§4 rule 7) — and "neither" is a
   > reportable result. What was wrong was a ruling written from a report without recomputing its
   > numbers or asking what its quotations left out, by the manager.

   **IIIA is modelled with the IIF law, as a named stopgap**, which is what IGS analysis centres
   without a IIIA model do; the true law — reported in 2023 to resemble IIR's but with a smaller
   maximum rate and an earlier start — is carried beside Galileo, GLONASS and BeiDou with its
   source located. Deciding factor: **no frozen baseline consumes a IIIA attitude law, and in fact
   none consumes any attitude law** — the `G-*` fits are cannonball fits and `B-IIIA` is a block
   mean of the cannonball scale (`oracle/ORACLE.md` §6). So this step's attitude laws serve the
   box-wing modelling of step 5, a capability beyond the predecessor's baselines, and its gate is
   published turn behaviour — the β₀ relation, continuity at hand-over, the rate bound, and
   attitude from openly published ORBEX files on a date the satellite is actually inside its turn
   regime — not the frozen residuals. *Amended the same day:* this said "observed attitude", and
   whether an ORBEX file carries a measurement or the analysis centre's own model is a search
   (§4 rule 4) the manager had not made. If it is the centre's model, the comparison is §4 rule
   8's — against another implementation of some reading of the same sources — and is named so.

   *And a boundary on sources:* the `eclips` Fortran is **code** whose licence is not established;
   it cross-checks parameters and is never a template. The laws are implemented from Kouba 2009,
   Dilssner 2010 and Montenbruck 2015.

