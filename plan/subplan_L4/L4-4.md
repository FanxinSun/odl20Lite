# L4 step 4 — `drag`

Step 4 of layer **L4** in [`../PLAN.md`](../PLAN.md) §3.5. Status: **DONE** (gate closed 2026-09-23).
Paths are relative to `rewrite/`, the rewrite's tree, as they are in the plan.

---

4. **DONE** — `drag`: the drag force over L2's atmosphere, with the drag coefficient a
   **registered parameter from the first commit**, never a constant. Gate: published ballistic
   coefficient cases, and the parameter's sensitivity column against finite differences.

   **Gate closed 2026-09-23**, `ci.sh` exit 0, 13 gates, 279 tests. The position Jacobian's
   residual was traced — through three explanations that were each rejected with their reasons —
   to NRLMSISE-00's seven species-correction cutoffs, which the frozen reference shares exactly
   (§3.3 step 4). Four items closed it. A jump table at stated conditions: relative density jumps
   of 4 to 28 × 10⁻⁵, and acceleration jumps falling four orders from N₂ to N with |**a**| itself.
   A runtime stencil that never straddles a cutoff: one-sided second-order at the same cost,
   `DRAG-R-012`, proven by `DRAG-A-011` at a case built to straddle, because `DRAG-A-010`'s own
   point cleared its nearest cutoff by 37 m of luck. The integrator effect sized rather than
   asserted, and carried to L7 with the target spacecraft's scaling (§3.8). And the diagnosis
   promoted into `atmosphere`'s own suite, both sides of every cutoff, against frozen reference
   output.

