# L5 step 2 — Galileo

Step 2 of layer **L5** in [`../PLAN.md`](../PLAN.md) §3.6. Status: **DONE**.
Paths are relative to `rewrite/`, the rewrite's tree, as they are in the plan.

---

2. **DONE** — Galileo, from the ESA GSC metadata published in 2017 — dimensions, mass, centre
   of mass, optical coefficients and attitude law.

   *Rule-4 and licence search, 2026-09-24.* The European GNSS Service Centre's metadata page
   prints everything this step needs on the page itself — the reference frame per IOV and FOC, the
   attitude law's equations in the operator's frame and again converted to the GPS/ANTEX
   convention, per-satellite dated mass and centre-of-mass histories, and per-surface optical
   coefficients — retrievable without an account. Its terms authorise reproduction and use
   "provided the source is acknowledged as follows: © EU 2011-2026", so the data is first-party
   and redistributable with that acknowledgement.

   *Ruled the same day.* **The frame:** Galileo's +x points to deep space, not the Sun, so the
   operator's frame is mapped to this tree's one convention by a tested mapping, the published
   conversion checked rather than trusted. **The attitude law** is implemented once and checked
   against both of the operator's printed forms through that mapping — two printed forms are an
   independent check the GPS laws never had — and carries step 6's two guards from the start, time
   running forward and turning the way the nominal turns, each shown firing. **The optical
   letters** are pinned by the operator's own definitions, quoted, and by each material's three
   coefficients summing to one, since `RS14` showed letters differ between sources. **Mass and
   centre of mass** are dated per satellite in the source and the schema holds one value, so the
   library returns a macromodel for a satellite at an epoch — surfaces and optics from its block,
   mass and centre of mass from the entry valid at that epoch — and refuses an epoch outside the
   table; the schema is populated, not extended, and the same shape serves GPS's per-satellite
   masses later (`SPCR-Q-002`).

   *Reviewed 2026-09-24, after the first build.* Built as ruled: the frame by a mapping checked
   against the operator's own paired coordinates, the optics by its quoted definitions and each
   material summing to one, mass and centre of mass by epoch, refused outside the table. The
   operator's IOV and FOC laws reduce to the nominal law L4 already gates, which the operator's
   second printed form confirms. **Three things remain.** *FOC's modified yaw steering is built:*
   its window, |β| < 4.1° within 10° of noon or midnight, is Galileo's noon and midnight turn,
   where the nominal yaw swings through nearly 180°, not a corner — the regime step 6 was about for
   GPS — and a law that needs the moment its window opened computes that moment from the geometry,
   as the IIF's shadow turn does; if the operator's text defeats that, the reason is reported
   first. *Step 6's two guards are built as step 6 built them:* continuity and a bounded rate are
   different checks, and neither sees time run backwards or a turn go the wrong way. *A real-data
   control*, CODE's Galileo attitude through a low-β crossing for an IOV satellite and for an FOC
   one, reproducible, not gated — the GPS law's worst defect was found only that way. IOV's
   end-of-life optics are selected explicitly, with no default, since every IOV satellite is long
   past early life; masses for satellites the operator does not list stay refused and carried.

   *Closed 2026-09-24.* IOV and FOC are populated from the operator's own metadata — the frame by
   a mapping checked against its paired coordinates, the optics by its own definitions with an
   explicit choice of beginning- or end-of-life for IOV, mass and centre of mass by epoch — and
   both attitude laws are built: IOV's smoothing and FOC's modified yaw steering, the latter
   stateless because its window depends on the orbit angle alone. Step 6's two guards are built as
   step 6 built them, and they found that IOV's law cannot run backwards at all, since it never
   reads the velocity. CODE's Galileo attitude, with predictions and a 2° criterion registered
   before any quaternion was read, agrees within 0.002° for E11 and within 0.1° for E33 — the
   first real-data check either law had. Verified by the manager, not taken on report: `ci.sh`
   exits 0 on `fb7d870`, 13 gates, 349 tests, 696 artefacts byte-identical. Carried: the FOC
   residual's cause, one satellite pair on one day, and masses the operator does not list.
